#ifndef INSTALLER_HPP
#define INSTALLER_HPP

#include <string>

// Install headers from specified path to include directory
void install_headers(const std::string& from_path);

#endif // INSTALLER_HPP
